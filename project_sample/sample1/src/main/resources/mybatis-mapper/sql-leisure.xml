<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.sample1.mapper.LeisureMapper">
	<select id="selectLeisureList" parameterType="hashmap" resultType="com.example.sample1.model.Product">
		SELECT *
		FROM T2_LEISURE_TBL R
		INNER JOIN T2_CODE_TYPE C ON R.LEISURE_KIND = C.C_KIND
	</select>
	
	<update id="updateLeisure" parameterType="hashmap">
		UPDATE T2_LEISURE_TBL
			SET 
				LEISURENAME = #{leisureName},
				LEISUREPRICE = #{leisurePrice},
				L_ADDR = #{lAddr},
				L_DETAIL_ADDR = #{lDetailAddr}
			WHERE LEISURE_NO = #{leisureNo}
	</update>
</mapper>